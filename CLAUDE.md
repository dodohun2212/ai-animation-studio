# Claude Project Instructions

Before working in this repository, read and follow these files in order:

1. `AGENTS.md` for project-specific requirements and safety rules.
2. `AI_GUIDELINES.md` for shared implementation behavior.

If the instructions conflict, follow `AGENTS.md` because it contains the
project-specific constraints.

Do not assume ownership of frontend or backend files until the user assigns a
worktree and role for the current session.
