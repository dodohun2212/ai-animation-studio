import { useEffect, useRef, useState } from "react";
import type { LongEpisodeDetail, LongEpisodeScript } from "@ai-animation-studio/shared";
import { approveLongEpisodeScript, generateLongEpisodeScript, getLongEpisode, toLongProjectDisplayError, updateLongEpisodeScript } from "../api/longProjectsApi.js";

interface Props { projectId: string; episodeNumber: number; onBack: () => void; onOpenMappingReview?: (projectId: string, episodeNumber: number) => void; }
type ErrorState = { code: string; message: string };
const fields = ["description", "visualAction", "startMotion", "mainMotion", "endMotion", "shotSize", "cameraAngle", "composition", "lensFeel", "focusSubject", "cameraMotion", "environmentMotion", "motionSpeed", "motionIntensity", "expressionChange", "continuityHint"];
function isScript(value: unknown): value is LongEpisodeScript { if (!value || typeof value !== "object" || Array.isArray(value)) return false; const item = value as Record<string, unknown>; return typeof item.title === "string" && typeof item.synopsis === "string" && typeof item.ending === "string" && Array.isArray(item.scenes) && item.scenes.length === 6 && item.scenes.every((scene, index) => !!scene && typeof scene === "object" && !Array.isArray(scene) && (scene as Record<string, unknown>).number === index + 1 && fields.every((field) => typeof (scene as Record<string, unknown>)[field] === "string")); }

export function LongEpisodeScriptScreen({ projectId, episodeNumber, onBack, onOpenMappingReview }: Props) {
  const [episode, setEpisode] = useState<LongEpisodeDetail | null>(null); const [text, setText] = useState(""); const [loading, setLoading] = useState(true); const [pending, setPending] = useState(false); const [error, setError] = useState<ErrorState | null>(null); const [confirming, setConfirming] = useState(false); const busy = useRef(false);
  const apply = (next: LongEpisodeDetail) => { setEpisode(next); setText(next.script ? JSON.stringify(next.script, null, 2) : ""); setError(null); };
  useEffect(() => { let cancelled = false; setLoading(true); getLongEpisode(projectId, episodeNumber).then((response) => { if (!cancelled) apply(response.episode); }).catch((caught) => { if (!cancelled) setError(toLongProjectDisplayError(caught)); }).finally(() => { if (!cancelled) setLoading(false); }); return () => { cancelled = true; }; }, [projectId, episodeNumber]);
  async function run(action: () => Promise<{ episode: LongEpisodeDetail }>) { if (busy.current) return; busy.current = true; setPending(true); setError(null); try { apply((await action()).episode); } catch (caught) { setError(toLongProjectDisplayError(caught)); } finally { busy.current = false; setPending(false); } }
  function parsed(): LongEpisodeScript | undefined { try { const value: unknown = JSON.parse(text); if (isScript(value)) return value; } catch { /* surfaced below */ } setError({ code: "INVALID_REQUEST", message: "대본은 정확히 6개의 순서가 맞는 장면을 포함한 JSON이어야 합니다." }); return undefined; }
  const hasScript = Boolean(episode?.script);
  return <section className="mt-8 space-y-4"><button type="button" className="rounded-full border border-white/10 px-4 py-2 text-sm text-slate-300" onClick={onBack}>프로젝트로 돌아가기</button><h2 className="text-xl font-semibold">Episode {episodeNumber} 상세 대본</h2>{loading && <p className="text-slate-400">불러오는 중...</p>}{episode && <p data-testid="episode-script-status" className="text-sm text-slate-400">상태: {episode.status} · revision {episode.scriptRevision} · history {episode.scriptHistoryCount}</p>}{!hasScript && episode?.status === "outline_ready" && <button type="button" className="rounded-full bg-violet-500 px-4 py-2 text-sm font-semibold text-white disabled:opacity-50" disabled={pending} onClick={() => void run(() => generateLongEpisodeScript(projectId, episodeNumber, {}))}>{pending ? "생성 중..." : "Local 대본 생성"}</button>}{hasScript && <><label className="block text-sm text-slate-300" htmlFor="episode-script-json">대본 JSON<textarea id="episode-script-json" className="mt-1 min-h-96 w-full rounded-lg border border-white/10 bg-slate-800 px-3 py-2 font-mono text-xs text-slate-100" value={text} disabled={pending || episode?.status !== "script_review"} onChange={(event) => setText(event.target.value)} /></label>{episode?.status === "script_review" && <div className="flex gap-3"><button type="button" className="rounded-full border border-white/10 px-4 py-2 text-sm text-slate-300 disabled:opacity-50" disabled={pending} onClick={() => { const script = parsed(); if (script) void run(() => updateLongEpisodeScript(projectId, episodeNumber, { script })); }}>수정 저장</button><button type="button" className="rounded-full border border-amber-400/40 px-4 py-2 text-sm text-amber-300 disabled:opacity-50" disabled={pending} onClick={() => setConfirming(true)}>대본 승인</button><button type="button" className="rounded-full border border-white/10 px-4 py-2 text-sm text-slate-300 disabled:opacity-50" disabled={pending} onClick={() => void run(() => generateLongEpisodeScript(projectId, episodeNumber, { regenerate: true }))}>새로 생성</button></div>}{confirming && <div role="alertdialog" data-testid="episode-script-approve-confirm" className="space-y-3 rounded-lg border border-amber-400/40 bg-slate-900 p-4"><p className="text-sm text-amber-300">이 대본을 승인할까요? 다음 Asset mapping 단계는 아직 시작하지 않습니다.</p><div className="flex gap-3"><button type="button" className="rounded-full border border-white/10 px-4 py-2 text-sm text-slate-300" disabled={pending} onClick={() => setConfirming(false)}>돌아가기</button><button type="button" className="rounded-full bg-violet-500 px-4 py-2 text-sm font-semibold text-white" disabled={pending} onClick={() => void run(async () => { const response = await approveLongEpisodeScript(projectId, episodeNumber, { approved: true }); setConfirming(false); return response; })}>최종 승인</button></div></div>}{episode?.status === "script_approved" && <div className="space-y-2"><p className="text-sm text-emerald-400">대본이 승인되었습니다.</p>{onOpenMappingReview && <button type="button" className="rounded-full border border-violet-400/40 px-4 py-2 text-sm text-violet-200" onClick={() => onOpenMappingReview(projectId, episodeNumber)}>Asset mapping 검토</button>}</div>}</>}{error && <p role="alert" data-error-code={error.code} className="text-sm text-rose-400">{error.message}</p>}</section>;
}
